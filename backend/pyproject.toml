[tool.poetry]
name = "aurora-backend"
version = "0.1.0"
description = "AuroraOS backend"

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.95.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
